import React from "react";
import styled from "styled-components";

const Summary = () => {
    return <Container>Summary Page</Container>;
};

export default Summary;

const Container = styled.div`
    display: flex;

    flex: 0.7;
    background-color: var(--chatscreen-bg-color);
    margin-top: 60px;
`;
